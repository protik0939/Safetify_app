import React from 'react';
import { View } from 'react-native';

// This is a placeholder screen for the SOS button position in the tab bar
export default function SOSPlaceholder() {
  return <View />;
}
